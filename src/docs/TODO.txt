In readme:
    Why Oracle is not tested
        Huge download
            Also required for Docker image
        Not in package managers
        Rarely used
        I hate Oracle

    Backends for which "char" field seem valid:
        mysql
        oracle
        postgresql
        sqlite3
        SQL anywhere
        IBM DB2
        MS SQL Server
        Firebird

    Backend behavior for timestamp field
        New auto_now_update keyword arg
        MySQL/MariaDB combinations
            Removal of application-layer auto_now functionality
            Set default to 0 or None to disable all TIMESTAMP behavior
            auto_now_add + auto_now_update = auto_now
        Postgre timestamp without timezone (default Django behavior)
        sqlite3 datetime (default Django behavior)
        Oracle?

FixedCharField
    Ensure that null keyword argument in FixedCharField actually stoes NULL in database?

TimestampField
    Add auto_now_update keyword argument

Before release:
    Run style checker and linter
